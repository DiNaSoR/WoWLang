# WoWLang / WoWAR – Development Journal

## 2026-01-01
- [Assets][ControlCenter] Removed unused `WoWAR/Images/ControlCenter/*` files.
  - Why it changed: reduce addon size and eliminate unused preview/art assets.
  - Key files touched:
    - `WoWAR/Images/ControlCenter/*` (deleted 73 unused files; kept only assets referenced by `common/Config/ControlCenter/*` and current `Preview_<dbKey>.jpg` set)
  - Regression checks run (from `regression-checklist.md`): N/A (asset cleanup; recommended manual in-game open of ControlCenter to verify textures/previews load).

- [Assets][ControlCenter] Restored missing ExpansionLandingPage textures required for the in-game border.
  - Why it changed: ControlCenter’s outer NineSlice border is created by `LandingPageUtil.CreateExpansionThemeFrame()` and relies on `Images\\ExpansionLandingPage\\ExpansionBorder_TWW` (plus dropdown/highlight textures). If those files are missing, the “config border” disappears in-game.
  - Key files touched:
    - `WoWAR/Images/ExpansionLandingPage/ExpansionBorder_TWW.tga`
    - `WoWAR/Images/ExpansionLandingPage/DropdownMenu.tga`
    - `WoWAR/Images/ExpansionLandingPage/HorizontalButtonHighlight.tga`
  - Regression checks run (from `regression-checklist.md`): N/A (asset restore; recommended manual in-game open of ControlCenter to verify border renders).

- [Config][ControlCenter][Fix] Fixed “Show minimap icon” toggle stuck ON in ControlCenter.
  - Why it changed: the getter used the Lua idiom `a and b or default`, which breaks when `b` can be `false` (it falls through to `default`). This made the checkbox always evaluate to true.
  - Key files touched:
    - `common/Config/ControlCenter/Registry.lua` (minimap setting `get` now uses `not (p and p.minimap and p.minimap.hide)`).
  - Regression checks run (from `regression-checklist.md`): N/A (config-only; recommended manual in-game toggle ON/OFF to confirm it persists and shows/hides the minimap icon).

- [Config][ControlCenter][RTL] Made Release Notes (مذكرات الإصدار) render RTL for Arabic only.
  - Why it changed: ChangelogTab hard-coded LEFT justification and TOPLEFT anchoring, so Arabic release notes were not RTL-aligned.
  - Key files touched:
    - `common/Config/ControlCenter/SettingsPanel.lua` (Arabic-only RIGHT justification + TOPRIGHT anchoring for changelog content and version title).
  - Regression checks run (from `regression-checklist.md`): N/A (config UI; recommended manual open ControlCenter → Release Notes and verify Arabic text alignment/scrolling).

- [Config][ControlCenter][RTL] Kept the changelog version/date header LTR in Arabic.
  - Why it changed: mixed LTR (numbers/date) + RTL (Arabic label) was reordering badly; keeping this header LTR makes the version/date stable and readable.
  - Key files touched:
    - `common/Config/ControlCenter/SettingsPanel.lua` (AR-only uses `Version` for the header line and keeps it left-justified).
  - Regression checks run (from `regression-checklist.md`): N/A (config UI; recommended manual check Release Notes header for correct `Version 12.xx 01 Jan 2026` order).

- [Config][Localization] Added Arabic (easy) descriptions for all config options (Summary + Tip) and wired them into the UI.
  - Why it changed: config toggles lacked clear Arabic explanations, and ControlCenter entries had `description = nil`, so the right-side preview was empty.
  - Key files touched:
    - `WoWAR/WoW_Localization_AR.lua` (added/updated `...DESC` strings; added missing keys like `fontsize`, `timerDisplayDESC`, `ControlCenter_UI_TalentsUI_DESC`).
    - `common/Config/ControlCenter/Registry.lua` (added `description` for modules/subOptions; aligned key usage with existing localization keys).
    - `common/Config/Tabs/*` (added AceConfig `desc` fields and localized some hard-coded labels).
  - Regression checks run (from `regression-checklist.md`):
    - Tooltips: ensure no duplicate hook ownership changes were introduced (config-only; no runtime hook changes made).

- [Config][ControlCenter][RTL] Fixed AR option descriptions not being shaped + colored.
  - Why it changed: `QTR_ReverseIfAR` is defined later in the TOC (`common/Text.lua`), so config descriptions captured earlier could render without shaping; also Tips needed a distinct highlight color.
  - Key files touched:
    - `common/Config/ControlCenter/SettingsPanel.lua` (Arabic-only: shape preview descriptions via `QTR_ExpandUnitInfo`, right-justify/anchor the description area, and colorize “نصيحة/تلميح” lines in yellow).
  - Regression checks run (from `regression-checklist.md`): N/A (config UI; recommended manual verify option description text is RTL-shaped and tip line is yellow).

- [Plugins][Storyline][Fix][RTL] Hardened Storyline integration and improved Arabic UI.
  - Why it changed: Storyline’s `playNext()` expects `Storyline_NPCFrameChat.eventInfo` to exist; our reload calls could run while it was nil, causing an error. Also Arabic needed a proper font + RTL-feeling chat animation.
  - Key files touched:
    - `common/Plugins/StorylinePlugin.lua` (safe `playNext` retry + eventInfo hydration, AR-only font apply + RIGHT justification for chat text).
    - `common/Quests/Gossip.lua` (use Storyline chat FontString for shaping + AR_RIGHT mode).
  - Regression checks run: recommended manual open a gossip dialog with Storyline enabled and verify no errors, Arabic text renders, and the typewriter animation grows RTL.
